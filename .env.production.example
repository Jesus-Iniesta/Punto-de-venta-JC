# ===========================================
# CONFIGURACIÓN DE PRODUCCIÓN - PUNTO VENTA
# ===========================================
# COPIA ESTE ARCHIVO A .env.production Y COMPLETA CON VALORES REALES
# NO SUBIR A GIT CON VALORES REALES

# PostgreSQL
POSTGRES_USER=puntoventauser
POSTGRES_PASSWORD=CAMBIAR_ESTO_POR_PASSWORD_SEGURO
POSTGRES_DB=puntoventa_prod

# Redis (para sesiones)
REDIS_PASSWORD=CAMBIAR_ESTO_POR_PASSWORD_REDIS_SEGURO

# Backend JWT Secret
SECRET_KEY=GENERAR_STRING_ALEATORIO_LARGO_MINIMO_32_CARACTERES
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Puerto público (opcional, default: 80)
PORT=80

# ===========================================
# NOTAS DE SEGURIDAD:
# ===========================================
# 1. Genera SECRET_KEY con: openssl rand -hex 32
# 2. Genera REDIS_PASSWORD con: openssl rand -hex 16 
# 3. Usa contraseñas largas y únicas
# 4. NO uses estos valores de ejemplo en producción
